<Window x:Class="RevitRotateAddin.RotateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rotate Elements / Xoay đối tượng" 
        Width="450" Height="255" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="RunButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="#388E3C" 
                                BorderThickness="1" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#388E3C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="#D32F2F" 
                                BorderThickness="1" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E53935"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#D32F2F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <!-- Khung bao bên ngoài với đổ bóng và góc bo tròn -->
    <Border Background="White" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1" 
            CornerRadius="8" 
            Margin="10,10,10,5">
        <Border.Effect>
            <DropShadowEffect Color="Gray" 
                              Direction="315" 
                              ShadowDepth="3" 
                              Opacity="0.3" 
                              BlurRadius="10"/>
        </Border.Effect>
        
        <StackPanel Margin="20,15,20,10">
            <!-- Tiêu đề nhập góc xoay -->
            <TextBlock Text="Enter rotation angle / Nhập góc xoay (degrees):" 
                       HorizontalAlignment="Left" 
                       Margin="0,0,0,5" 
                       FontWeight="SemiBold"/>
            
            <!-- Ô nhập góc xoay -->
            <TextBox Name="angleTextBox" 
                     Height="30" 
                     Margin="0,0,0,15"
                     Text="0" 
                     ToolTip="Nhập góc xoay bằng độ. Ví dụ: 90, -45, 180"
                     VerticalContentAlignment="Center"
                     HorizontalAlignment="Stretch"
                     FontSize="14"
                     BorderBrush="#CCCCCC"
                     BorderThickness="1"
                     Padding="8,0"/>
            
            <!-- Thông tin về trục xoay -->
            <TextBlock Text="Rotation axis / Trục xoay:" 
                       HorizontalAlignment="Left" 
                       Margin="0,0,0,5" 
                       FontWeight="SemiBold"/>
            
            <TextBlock Text="• You can select pipes (ống) or pipe fittings (phụ kiện ống) as rotation axis" 
                       HorizontalAlignment="Left" 
                       Margin="5,0,0,3" 
                       FontSize="11"
                       Foreground="DarkBlue"/>
            
            <TextBlock Text="• Bạn có thể chọn ống hoặc phụ kiện ống làm trục xoay" 
                       HorizontalAlignment="Left" 
                       Margin="5,0,0,15" 
                       FontSize="11"
                       Foreground="DarkBlue"/>
            
            <!-- Các nút với màu nền -->
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Button Name="btnRun" 
                        Content="Run" 
                        Grid.Column="0"
                        Style="{StaticResource RunButtonStyle}"
                        Height="32" 
                        Margin="30,0,10,0"
                        Click="btnRun_Click"/>
                
                <Button Name="btnCancel" 
                        Content="Cancel" 
                        Grid.Column="1"
                        Style="{StaticResource CancelButtonStyle}"
                        Height="32" 
                        Margin="10,0,30,0"
                        Click="btnCancel_Click"/>
            </Grid>
            
            <!-- Dòng chữ Version -->
            <TextBlock Text="Version 1.0 - Licorp" 
                       HorizontalAlignment="Center" 
                       Margin="0,5,0,0" 
                       FontSize="10"
                       Foreground="Black"/>
        </StackPanel>
    </Border>
</Window>
